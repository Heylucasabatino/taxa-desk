{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main desktop window permissions for Taxa Desk.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "dialog:default",
    "opener:default",
    "updater:default"
  ]
}
